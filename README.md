# requo releases

Public download repository for [requo](https://github.com/yanislavm/requo) releases.

## Install via Homebrew

```bash
brew tap yanislavm/requo
brew install --cask requo
```

All release assets (macOS DMG, Windows ZIP, Linux tarball, CLI binaries) are published here on each tagged release.
